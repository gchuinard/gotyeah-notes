# @blocknote/mantine@0.48 déclare un peer @mantine/core@^8, mais le projet est
# sur @mantine/core@9 (résolution déjà actée dans package-lock.json). Sans ça,
# `npm ci` (npm 11) échoue sur ERESOLVE. Vaut pour local, CI et build Docker.
legacy-peer-deps=true
